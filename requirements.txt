# POSEMAP - Pose-Oriented Single-particle EM Micrograph Annotation & Projection
# Python dependencies

# Core scientific computing
numpy>=1.20.0
scipy>=1.7.0

# File I/O
mrcfile>=1.3.0
h5py>=3.0.0

# Visualization
matplotlib>=3.4.0
Pillow>=8.0.0

# GUI
# tkinter is included with Python, no installation needed

# Structure parsing (optional but recommended)
biopython>=1.79

# Image processing
scikit-image>=0.18.0

# Note: PyMOL must be installed separately via conda:
# conda install -c conda-forge pymol-open-source
#
# Note: EMAN2 (optional, recommended for higher quality projections) via conda:
# conda install -c cryoem -c conda-forge eman-dev
# POSEMAP will automatically use EMAN2 if available, otherwise falls back to NumPy-based projections
#
# Note: ChimeraX must be installed separately:
# Download from https://www.rbvi.ucsf.edu/chimerax/

